const { Symbol } = require("../db/index");
const { historicalData } = require("../data/historicalDailyData");

function analyzeSymbols(req, res, next) {
  // make calls to alpah vantage api here and
  // send anaylsis to db model
  //
}

module.exports = analyzeSymbols;
